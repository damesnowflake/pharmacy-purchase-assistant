# Cloudflare Pages 설정. 개발계획.md: GitHub Pages 대신 Cloudflare Pages Free를 배포 대상으로 한다.
# 실제 배포는 Cloudflare 계정 연결이 필요하다 (docs/미확인_항목.md). 이 파일은 `wrangler pages deploy`
# 또는 Cloudflare 대시보드의 "기존 설정 가져오기"로 프로젝트를 만들 때 참고용이다.
name = "pharmacy-purchase-assistant"
compatibility_date = "2026-09-23"
pages_build_output_dir = "dist"
